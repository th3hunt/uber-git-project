const uberGit = {

  greet(name) {
    return `hello, ${name!}`;
  },

  shout(words) {
    return `${words.toUpperCase()}!!!`;
  },

  whisper(words) {
    return `${words.toLowerCase()}...`;
  }

};

export default uberGit;
